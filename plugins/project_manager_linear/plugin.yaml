name: project-manager-linear
version: 1.4.0
description: >-
  Deterministic PM Linear lane with direct writable-state transitions, reserved
  duplicate relation handling, owner-approved destructive mutations, and
  bounded ordered bulk orchestration.
author: sisyphus-org
kind: backend
provides_tools:
  - pm_linear_execute
