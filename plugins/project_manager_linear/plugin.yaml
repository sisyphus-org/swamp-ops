name: project-manager-linear
version: 1.6.1
description: >-
  Deterministic PM Linear lane with compare-before-set issue moves, direct
  writable-state transitions, reserved duplicate relation handling,
  trusted delete previews, owner-approved destructive mutations, bounded ordered bulk orchestration,
  and deterministic recovery from transient issue-create failures.
author: sisyphus-org
kind: backend
provides_tools:
  - pm_linear_execute
