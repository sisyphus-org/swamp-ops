name: ops-broker
version: 0.6.1
description: >-
  Typed, policy-allowlisted operations broker for authenticated A2A peers.
  Adds owner-only Swamp attestation for bounded destructive Linear operations without granting Linear mutation capability.
author: sisyphus-org
kind: backend
provides_tools:
  - ops_broker
