name: ops-broker
version: 0.8.0
description: >-
  Typed, policy-allowlisted operations broker for authenticated A2A peers.
  Adds owner-only Swamp attestation and opaque preview confirmation for bounded destructive Linear operations without granting Linear mutation capability.
author: sisyphus-org
kind: backend
provides_tools:
  - ops_broker
