name: ops-broker
version: 0.1.0
description: >-
  Typed, policy-allowlisted operations broker for authenticated A2A peers.
  Exposes read-only GitHub and Swamp operations without sharing credentials.
author: sisyphus-org
kind: backend
provides_tools:
  - ops_broker
