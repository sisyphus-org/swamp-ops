name: ops-broker
version: 0.9.0
description: >-
  Typed operations executor for the dedicated broker profile, with a separate
  policy-limited owner approval bridge for the default profile.
author: sisyphus-org
kind: backend
provides_tools:
  - ops_broker
